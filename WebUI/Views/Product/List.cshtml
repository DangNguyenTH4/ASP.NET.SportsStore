@model WebUI.Models.ProductsListViewModel

@{
    ViewBag.Title = "List";
}
@foreach(var p in Model.Productsss)
{
    @Html.Partial("ProductSummary", p)
}
<div class="btn-group pull-right">
    @Html.PageLink(Model.PagingInfo, x => Url.Action("List", new { page = x }))
</div>